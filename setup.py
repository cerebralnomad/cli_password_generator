from setuptools import setup, find_packages

setup(
   name='newpass',
   version='1.0',
   packages=find_packages(),
   scripts=['newpass.py'],
   description='Generate random passwords from the command line',
   author='Cerebral Nomad',
   author_email='cerebralnomad@protonmail.com',
   
)
